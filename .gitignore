node_modules/
dist/

# ★スクリーンショットは【検査の出力】であって成果物ではない。
# `npm run smoke` と `probe-look.ts` が毎回書き換えるので、
# 差分がノイズになる。必要なときだけ手で追加する
snapshots/

# タイムラプスの生データ（39MB）。再生成できる
public/timelapse.bin
public/timelapse.json

# 系統樹の確認用データ（probe-phylo.ts が生成する）
public/phylo.json

# ★挿絵の受け取り箱（ChatGPT の既定名のまま）。
#   `scripts/art/install-illust.py` が 512x288 に落として
#   `public/illust/` へ入れるので、**元は追跡しない**（1 枚 1.2MB・25 枚で 32MB）。
#   ★`docs/images/` の他のファイル（README のスクショ）は追跡する
docs/images/ChatGPT*
