# Gaia Protocol

45.4 億年の惑星シミュレータ。気候・炭素循環・水循環・プレートテクトニクスを
結合して回し、生命と文明までを扱う（`docs/00-concept.md`）。

## ★ 作業を再開するとき

**「つづき」と言われたら、まず `WORK-IN-PROGRESS.md` の冒頭を読むこと。**

冒頭に **「いまの状態」**（テスト・監査の結果、既定に入っている変更、
既定 0 のまま止まっている機構）と **「読む順序」** の表がある。
**1 番目に挙がっているセクションが次にやること。** そこから始める。

★**ファイルは 1700 行ある。頭から順に読まないこと。** 読む順序の表に従う。
古いセクションは経緯であって、必要になったときだけ読めばよい。

各機構の A/B の実測表と有効化の条件は、**パラメータのコメント**に書いてある
（`ridgeFillByCoverage` など）。コードを読めばその調査の結論が分かる。

片付いたら消してよい。再び中断するときは書き直して残すこと。
**そのとき冒頭の「いまの状態」と「読む順序」も更新すること。**

## ブラウザで動かす

```bash
npm run dev     # → http://localhost:5180/  既定 128x64・CPU・「惑星 ★」レイヤ
npm run smoke   # 通し確認（要 npm run dev）
```

速度は**絶対値の段**（×1 = 10万年/秒 … ×20 = 200万年/秒 = 物理上限）。
どの時代でも同じ年数/秒で、表示は設定値（実測レートは揺れるので出さない）。
**「▶ 次の出来事まで」**は何も起きない時代を飛ばす。

★**気候が解けなくなったら赤いバナーが出る。** 出たら速度を落とすか seed を変える。
無言で 491℃ を出し続けることはもう無い（2026-09-01 に直した）。

## 常設の検査

```bash
npm test                              # 物理テスト（約 10 分）
npm run audit -- --width 96           # 総合監査（収支・停止検出・数値解法・軌跡）
npm run audit -- --width 96 --subgrid # サブグリッドの陸を強制して監査（A/B 用）
npm run audit -- --width 96 --timelapse   # タイムラプスも同時に出す
npx vite-node scripts/probes/probe-epoch.ts --seed audit --breakdown  # ★全史の時代別中央値
npx vite-node scripts/probes/probe-resmargin.ts --seed audit          # ★解像度独立性の余裕
npx vite-node scripts/probes/probe-landmass.ts   # 監査の後に陸のまとまりを測る
npx vite-node scripts/probes/probe-relief.ts --seed audit   # ★起伏の収支（M4 の主指標）
npx vite-node scripts/probes/probe-crustflux.ts --width 96   # ★地殻の生成/消費 [km³/yr]（数秒）
npx vite-node scripts/probes/probe-events.ts --seed audit    # 出来事の頻度（ゲーム性の判断材料）
npx vite-node scripts/probes/probe-look.ts                   # 時代ごとの見た目を PNG に
npx vite-node scripts/probes/probe-pixel.ts                  # ★ドット絵の見た目と【色数】（3.5 秒）
npx vite-node scripts/probes/probe-pixel.ts --years 3e8      # 氷のある惑星で見る（5 分）
npm run smoke                         # ブラウザ通し確認（要 npm run dev）
npx vite-node scripts/ui-shot.ts layer-picker  # ★押さないと出ない部品を撮る
                                      #   （settings / phylogeny / armed / diag / saves / science）
npx vite-node scripts/ui-shot.ts diag 430,932  # ★スマホの幅で撮る（第 2 引数が画面）
npx vite-node scripts/probes/probe-mobile.ts   # CPU を絞って重さを測る（要 npm run dev）
npx vite-node scripts/probes/probe-thc.ts        # 熱塩循環の較正とヒステリシス
npx vite-node scripts/probes/probe-gyre.ts       # 湧昇・西岸境界流（数十秒）
npm run gpu-verify                    # GPU 版と CPU 版の数値一致（要 npm run dev）
```

全史 1 本の目安: 64x32 で 10 分、96x48 で 29 分、128x64 で 60 分。
（2026-08-29 に地殻を粒子表現にして +59%。粒子 20 万個の回転とラスタライズの分）

## ★ 全史ランは並列で回すこと

このマシンは 24 論理コア（Ryzen 9 7900X）・62GB。Node は 1 プロセス 1 スレッド
なので、条件ごとにプロセスを分ける。**実測: 10 本並列で壁時計 411 秒
（1 本単独 340 秒）= スループット 8.3 倍。**

```bash
for c in 0 1 2 3; do
  npx vite-node scripts/probes/probe-shatter.ts --width 96 --case $c > /tmp/s-$c.log 2>&1 &
done
wait
```

## 測り方の約束（何度も踏んだ罠）

1. **数字には必ず条件を添える**（解像度・結合間隔・パラメータ・seed）。混ぜると誤診する
2. **単一ランの終端値で判断しない。** 中央値と分位点で見る
3. **指標の誤差を測ってから差を議論する。** 例: 有効陸塊数は seed 間で SD 3〜4 あり、
   単一 seed の比較は無意味。断片化を論じるなら 4 seed 以上・対応のある比較で
3b. ★**陸地面積で機構の良し悪しを判定しない**（seed 間 SD 3.8・相対 SE 13%）。
   代わりに **`probe-relief.ts` の「厚さの分散の時間中央値」**（相対 SE 2.9%）を使う。
   **目標は地球の 218 km²**（大陸 37km×41% + 海洋 7km×59%）。モデルはいま 76
4. **指標が「減った」ことを改善と読まない。** 母数が消えていないかを先に確認する
5. **指標そのものが解像度独立かを先に確かめる。** 陸塊の数はセル数に比例した
6. **保存量が合っていても安心しない。** 体積を 1km³ もこぼさずに地形が消えた
   バグが 2 件あった。**分布の形（分散）も台帳に載せること**（`varLedger`）
7. **テストの PASS を機構が正しい証拠にしない。** 数値ノイズで通っていた例が 2 件
8. **断片化の調査は 96x48 以上で。** 64x32 では断片化が飽和せず順位が逆転する
9. **総量で判定し、面積や強さで判定しない。** 海嶺も沿岸湧昇も【線】なので、
   セルを細かくすれば面積は減り強さは増える。不変なのは積分量の方
10. **離散式は解析解のある極限で検算する。** 剛体回転の発散は厳密に 0、
   一様な北向き流の発散は −c·tanφ/R。これで南北微分の符号バグを見つけた
   （y=0 が北極なので y は【南】へ増える。`docs/01-2.1`）
11. ★**「数学的に等価な書き換え」でも最終桁は変わる。45 億年積むとカオスで
   別の惑星になる。** 実測: `-(2600+350√age)/1000` を `-2.6-0.35√age` に
   書き換えただけで、20 億年後の陸地面積が **14.0% → 11.0%**、厚さの分散が
   91.4 → 71.8 になった。**単一ランの差はビット単位の変更でも 3 ポイント動く。**
   だから A/B は必ず 4 seed 以上・対応のある比較で。監査の全史 1 本の数字も
   「その 1 本の値」であって、モデルの代表値ではない

12. ★**まず基準状態（現在の地球）を測る。** 数秒で済み、「パラメータが悪い」と
   「時間発展で壊れる」を一撃で分ける。2026-08-30 の寒冷バイアス調査では、
   基準状態が アルベド 0.2905 / 15.36℃ / 氷 8.4% / 陸 29.0% と**完璧**だと
   分かった瞬間に、探す場所が全史の側に絞れた。**この日いちばん効率の良い測定**
13. ★**表に「変わるはずの量」を 1 列入れる。** プローブが監査と違うシナリオを
   走らせていても、気温も陸も CO2 も**もっともらしい値**が出るので気づけない。
   実際 `startEpoch: "hadean"` を渡し忘れて数時間を溶かした。気づけたのは
   日射の列が全時代 1361 のままだったから。**カナリアを置くこと**
14. ★**量を変える前に「これは誰の較正の分母か」を確かめる。** `crustProduction`
   を粒子の実測に差し替えたら、**陸地面積の解像度差が 104%** になった
   （契約は 20% 未満）。脱ガスの正規化 `prod / presentCrustProduction` の
   分子だけが変わり、推定器が食い違ったため。**症状は原因から遠くに出る。**
   自分が考えている名前（`ventFlux`）ではなく、**変える量そのものの名前**で grep する
15. ★**残差を 2 回手計算したら、診断量を足す。** アルベドの内訳を混合モデルで
   3 回見積もって「+0.039 が説明できない」と悩んだ。`reflSurface`/`reflIce`/
   `reflHaze` を `ClimateStats` に出したら 1 回の測定で決着した（ヘイズだった）
16. ★**「構造的だから作り直しが要る」は仮説であって診断ではない。** 熱水が
   桁で足りない原因を「プレート境界が地殻組成と独立だから、1 日以上の作り直し」
   と見積もったが、実体は `gap = hOc - thick[c]` の**1 行**だった。
   **数字を出している行を読むのが先。** システムの物語を作るのは後
17. ★**時代で効きが変わる量を、1 時点で検定しない。** セル平均マスクの人為性を
   冥王代で測って 48% vs 51.6%、差が無いので「外れ」と判定した。実際には
   顕生代で 13% vs ~100% の主因だった。**症状が時代依存なら検定も時代分解で**
18. ★**解像度依存のノイズを消す補正が、解像度依存の形をしていないか。**
   被覆率のポアソン揺らぎ（1/√n）を消そうと σ=1/√n を引いたら、
   **消したいノイズより大きい系統バイアス**が入って解像度差が 3 倍悪化した
19. ★**監査が PASS でも打ち消し合いを疑う。** 2026-08-30 に 3 層見つかった——
   陸が半分しか出ない誤り ↔ 気候の寒冷バイアス、CH4 100ppm の温室効果 +4.4K ↔
   誤点灯したヘイズの -4.4K。**失敗は直せるが、打ち消し合いは成功に見える。**
   機構を 1 つ直すと隣が壊れたときは、直したのではなく**釣り合いを崩した**のかもしれない

20. ★**テストや監査と比べるなら、その設定をコピーする。組み直さない。**
   2026-08-30 に 3 回踏んだ: `startEpoch: "hadean"` の渡し忘れ、
   `climateCouplingYears: 200_000` の渡し忘れ、`resolution.test.ts` の条件を
   **4 か所とも**取り違え（解像度 96/144 を 64/96 に、seed、エポック、`maxOuter`）。
   **どれも、それらしい数字が出るので値では気づけない**（13 番のカナリアが要る理由）。
   プローブの先頭に「どのテスト/監査と同じ設定か」をコメントで書くこと

21. ★**割合を真偽値として使っていないか。** `landFraction` は 0..1 なのに
   `isLand ? alphaLand : alphaOcean` と書いてあり、**セルの 1% でも陸なら
   陸のアルベドが丸ごと乗っていた**（`climate.ts`）。既定では lf が厳密に 0/1
   なので、テストも監査も `gpu-verify` も通る。**GPU 版は最初から `mix()` で
   混ぜていた**のに、CPU 側のコメントには「GPU 版と同じ式」と書いてあった。
   サブグリッドを有効にした瞬間だけ 3.2K の寒冷バイアスとして出た
22. ★**契約の判定量そのものが seed で暴れていないか。** 解像度独立性の CO2 は
   seed で 0.03〜0.22 と動き、**既定の構成でも seed を変えれば契約を破る**。
   その 1 本の値で 4 つの機構を 1 日以上止めていた。**判定は seed をまたいだ
   中央値で**。ついでに、判定量は増幅の小さい方を選ぶ（CO2 は制御出力で 5〜6 倍）
23. ★**「量は分数・判定は離散」に分けるとき、離散側の基準を隣の機構と揃える。**
   D8 の経路は `elevation` で解くのに陸判定だけ `landFraction >= 0.5` にしたら、
   **別の集合になって陸の 99.4% が河川網から外れた**（粒子の厚さ分布が右に裾を
   引くので、セル平均は海面を超えるのに過半数の粒子は超えない）。
   症状は「炭素の較正が合わない」という遠いところに出た

24. ★**正規化されて入る量の絶対値を、解像度依存の証拠にしてはいけない。**
   `erosionRef`（基準状態の侵食）が 96x48 と 144x72 で 1.75 倍ずれているのを
   見つけて「本命はこれ」と判断したが、外れだった。侵食は `carbon.ts` で
   `ero / erosionRef`、`tectonics.ts` で `ero / eroMean` と**どちらも正規化して
   使う**ので、一様な倍率は完全に打ち消える。しかも 1.75 倍の正体は
   **傾斜の式がセル幅で割っていない**という単位の欠落そのものだった
   （`slopePerDistance`）。**まず「その量はどう使われているか」を読む**
25. ★**幾何の自己調整を「churn の原因だから」と外してはいけない。** 海嶺の充填の
   被覆上限は、ポアズン揺らぎで churn する原因であると同時に**唯一の負の
   フィードバック**だった。運動学に置き換えたら粒子被覆が 250% で飽和した
   （`subductKinematic`）。**発火が揺らぐことと、量を止めることは別の役目**
26. ★**粒子 1 個単位の量子化は、セル数に比例して効く。** 「予算より大きい粒子を
   必ず消す」と 1 セルにつき最大 1 個の過剰が入り、収束セルは W² に比例するので
   消費が 45.8 → 118.0 km³/yr に膨らんだ。**端数は確率で処理すること**
   （海嶺の `nNew` は最初からそうなっていた）
27. ★**「解像度によらず固定」でよい標本数かを疑う。** 粒子 20 万個は解像度に
   よらず固定だったが、海嶺も沈み込みも**1 セルあたりの粒子数 n の揺らぎ
   （1/√n）**で発火が決まる規則だったので、n ∝ 1/W² が流束を変えていた。
   `parcelsPerCell` で 1 セルあたりを揃える。**128x64 で粒子 4 倍にしても
   速度低下は +14%**（コストは気候と水循環が支配する）

28. ★**測っているのに契約になっていない量は守られない。** `gpu-verify` は
   最初から `CPU 102ms / GPU 358ms` と印字していたが、**検査は一致（K）だけ**
   だったので、20 倍遅い GPU 経路を無条件に使い続けていた。
   **印字するだけでなく、閾値で落とすこと**
29. ★**「その装置が使えること」と「その装置が速いこと」は別。**
   気候ソルバは Newton + CG で**逐次**（内積の結果が次の一歩を決める）。
   1 solve が 753 ディスパッチになり、3.3 万セルでは**固定費が計算の 50 倍**。
   GPU に出す前に、**同じ初期状態から両方を計ること**（片方を先に走らせると
   その解が残った状態で相手を計ることになり不公平。これも踏んだ）
30. ★**安全装置を省いた実装を「同じ計算」と呼ばない。** CPU の擬似時間発展には
   「残差が悪化したら差し戻して刻みを縮める」「収束を判定する」があるが、
   GPU 版は反復固定でそれが無かった。平衡から遠い冥王代で別の枝に落ち、
   **温度が上限 500℃ に張り付いた**。しかも `converged: true` を決め打ち
   していたので**画面は無言だった**。**解けなかったことは必ず外へ出す**
31. ★**費用は「刻み」ではなく「結合間隔」で決まる。** `chunked` が
   `min(結合間隔, 残り)` で刻むので、**刻みを結合より小さくすると
   結合を粗くしても solve の回数が減らない**

32. ★**場をそのまま色にしない。** 深さを色にすると海底の凹凸が全部出て
   海が「くしゃくしゃの金属箔」に、氷の割合を白さに混ぜると氷縁が
   「破れた紙」に見えた。**表示は物理量の可視化であって転写ではない**。
   ただし**混合の重み（陸の割合など）は均してはいけない** ——
   均すと孤立した陸が薄まって**大陸がほとんど消える**（これも踏んだ）

33. ★**閾値は分布を測ってから置く。** 超大陸の分裂を分散度 0.50 で判定していたが、
   実測の **P95 が 0.486** で一度も跨がず、**全史で出来事が 0 件**だった。
   しかも初期判定で「集合済み」に固まるので、**黙って何も起きない**。
   「それらしい値」を置く前に、その量の分位点を測ること

34. ★**フィードバックの輪の長さが、その日の生産性をほぼ決める。**
   物理は「現在の地球で 5〜25 秒」の輪（`probe-crustflux.ts`）に落としてから
   一気に進んだのに、**描画は 35 分の輪（全史）で 10 周回して 5〜6 時間**溶かした。
   最後に作った「現在の地球を 1 枚だけ描く」（数秒）を**最初に作っていれば
   1 時間で終わっていた**。**測りたいものが決まったら、まず輪を短くする。**

35. ★**粒子からセル平均を作るときは、推定器の分散を意識する。**
   最近傍のビン分けは **1/√n の標本ノイズ**（97.7 粒子/セルで約 10%）を持ち、
   海洋地殻 7km に対して **±700m の偽の起伏**になる。それが海面付近で
   陸海を入れ替え、**大陸が断片化して見えた**。
   粒子を 4 倍にすると有効陸塊数が 4.6 → 2.0 になるので**切り分けは容易**。
   費用をかけずに直すなら**体積を保存する平滑化**（`rasterSmoothing`）
36. ★**「量」が合っていても「まとまり」は壊れていることがある。**
   陸地面積 20.5% は地球（29.2）に近いのに、分布はまるで違った。
   `probe-fragment.ts`（連結成分・最大の陸塊・有効陸塊数）で測ること

37. ★**「測るための平滑化」を物理に混ぜない。** `rasterSmoothing` は粒子から
   セル平均を推定する**測り方**であって拡散ではない。侵食が読む場に掛けたら、
   平滑化が粒子へ書き戻って **CO2 が 1Myr 周期で 200↔800ppm を往復**した
   （監査 C-1 のばらつき 1% → 108%）。**粒子へ戻る経路の手前では掛けない**
38. ★**地形を均すと風化のサーモスタットが弱る。** 起伏が減ると侵食が減り、
   **供給律速の割合が 2% → 14%** に上がる。供給律速では風化の温度依存が
   消えるので、負のフィードバックが効かなくなって CO2 が振動する。
   **「見た目のために均す」の上限は、C-1 の振動が出ない所**（実測 0.25 まで）
39. ★**既定値と同じ「省略」と「0 を明示」を区別すること。**
   `--smooth 0` を `if (SM > 0)` で受けていたので、**A/B の対照側が
   実験側と同じ設定で走っていた**。0 と 0.3 の行が 1 桁まで一致して初めて
   気づいた。**対照が本当に対照かを、値の一致で確かめる**
40. ★**「刻みで答えが変わる」の前に「振動していないか」を疑う。**
   監査 C-1 が刻み 25/100/400kyr で 870/205/615 を出したので刻み依存だと
   思ったが、実際は 3 本とも同じ振動をしていて**最後にどこを覗いたか**の差だった。
   軌跡を刻んで出せば 1 回で分かる（トラップ 2 の「終端値で判断しない」の再発）

41. ★**「下げることしかできない量」は、選択が絶対に採らない。** 好気呼吸は
   `oxOk = min(1, o2/demand)` だけで書かれていて、要求 0 が満点だった。
   コストだけで見返りが無いので採用 0/0/0/0、その結果
   **真核が 45 億年で一度も出なかった**。前提に使う形質は、
   **得をする条件が存在するか**を必ず確かめること
42. ★**0..1 の掛け算で組んだ適応度に「ボーナス」は足せない。** 1 で
   クランプされて区別が消える（過去に 16 クレードが全部同じ値になった）。
   有利を表したいときは**不利側を減点する**。そうすれば
   「その条件が無い時代」の較正も動かない
43. ★**要求量の目盛りを「現在の地球」で置かない。** 酸素要求を
   `trait × 21%` と書いたので、閾値のクレードが 4.6% の O2 を要求して
   適応度を潰していた。**パスツール点は現代の 1%**。
   生物が切り替わる濃度は、現在の大気とは桁が違う

44. ★**トレードオフの無い形質は必ず端に張り付く。** 上げるだけ得なら全員が上限へ、
   下げるだけ得なら全員が下限へ。実測で `photosynthesis` 1.000 / `nutrientP` 0.000 /
   `aridityTolerance` 1.000 が**すべて SD 0.000**だった。SD が出ていたのは
   トレードオフを持つ `tempOptimum` と `bodySize` だけ
45. ★**ただしトレードオフを足しただけでは分化しない。** 乾燥耐性にコストを付けたら
   1.000 → 0.000 へ**端から端へ飛んだだけ**で、間で分かれなかった。
   形質はクレードごとに 1 つの値、選択の点数は**全球の面積平均**なので、
   「乾いた土地に特化した系統」は生まれようがない。
   分化には**その形質が効く場所に系統を縛る機構**が別に要る
46. ★**「機構がある」と「機構が効いている」は別。** 能力ビット 9 個のうち
   **6 個は適応度のどこにも現れていなかった**（`capMotility` `capPredation`
   `capSkeleton` `capMulticellular` `capNitrogenFixation` `capSymbolic`）。
   多細胞を獲得しても何も変わらない。**`grep` で「その名前が何箇所で読まれるか」
   を数えるだけで分かる。** 進化の鎖を議論する前にこれをやること

47. ★**待ちループの `pgrep` は自分自身に一致する。** `while pgrep -f "probe-x"`
   と書くと、**そのシェルのコマンドラインに `probe-x` が含まれている**ので
   永久に終わらない。ブラケット形（`probe-[x]`）にすること。
   `pkill -f` で自分を殺した件（過去 2 回）と同じ根っこ
   ★**ブラケット形はパターン自身を守るだけ。** 同じコマンドラインの
   【別の場所】にその語があれば当たる。2026-09-03 に `pkill -f "[v]ite"` で
   自分のシェルを殺した —— 同じ行に `p='vite.config.ts'` が入っていた。
   **プロセスを殺すコマンドは、それだけを 1 回の呼び出しにする**

48. ★**UI を足したら必ずスクリーンショットを見る。** 神の手のボタンを足したら
   左の列が伸びて凡例と**重なり**、横並びにしたら 4 つ目が幅からはみ出して
   **黙って消えた**（`overflow-x: hidden`）。どちらもコードと型検査では出ない。
   `npm run smoke` は `snapshots/app-smoke.png` と `app-globe.png` を残すので、
   **それを開くまでは「入った」と言わないこと**

49. ★**新しいニッチを入れるときは「最初の 1 匹」が成立するかを確かめる。**
   栄養段階を入れたのに捕食者が 45 億年ずっと 0 だった。原因が 3 つ重なっていた——
   (a) 捕食者がいないとき餌の場を 0 にしていた（**鶏と卵**）、
   (b) 捕食者の候補を**生産者と同じ分母**で競争させていた、
   (c) 生態効率 10% が資源側で既にコストを課しているのに
   **捕獲効率でも二重に罰していた**。
   **機構を書いただけでは、そこへ移る勾配は生まれない**

50. ★**能力ビット 1 つでラベルを名乗らせない。** `capSymbolic` だけで
   「知性種」と表示したら **16 系統中 10 系統が知性種**になった（地球は 1 系統）。
   そのビットが**適応度のどこにも現れていない**と、獲得後は無償で子孫に広がる。
   **表示は、その裏に機構があるかどうかを勝手に保証してしまう**

51. ★**新しい制限を入れるときは、既存の生命が生き残れるかを先に確かめる。**
   非生物的な窒素固定を「生物固定の 1%」という**実際の比のまま**適応度に
   掛けたら、**4 seed すべてで生命が全滅した**。
   比が正しくても、それを適応度に直接掛けてよいとは限らない

52. ★**「抽選に通った」と「効果が出た」は別。** ハードステップの新機能化は
   複製元の遺伝子の**値をそのまま引き継ぐ**ので、能力の閾値（128）に
   届かず**通ったのに何も起きない**ことが多かった。実測で `capSymbolic` は
   提案 1〜4 回あって**採用 0/8**。full strength で入れたら、
   知性 1/8・真核 2/8→4/8・GOE 5/8→8/8 になった。
   **診断量は「引けたか」だけでなく「立ったか」まで数えること**

53. ★**枠を返すときは中身を消す。** 絶滅したクレードのレーンを
   `freeLanes` に返すだけで**場のバイオマスを消していなかった**ので、
   `biomassTotal` とレーンの合計がずれた（残差 1.25e-5）。
   再利用されるまで地図にも古い値が残る。
   **栄養段階でレーンの入れ替わりが増えて初めて表に出た** ——
   既存のテストが拾ってくれた
54. ★**機構の【入力】が不活性な量でないかを確かめる。** 生物起源の雲凝結核を
   `ccnProduction` の形質で駆動したら、その形質は副産物で選択が働かず 0 へ漂い、
   **「生命がいれば常に −0.03」という生命と無関係の定数**になった。
   自分で書いた罠 46 の入力側。**出力だけでなく入力も grep すること**

55. ★**アルベドに項を足したら、反射の台帳にも足す。** 生物起源の CCN を
   `raw` にだけ足して `absorbedSW = 入射 − (地表+氷+ヘイズ)` に入れ忘れたので、
   **放射の不平衡が中央値 4.7 W/m² のまま残った**（監査は 7.2e-5 → 4.7 で FAIL）。
   気温はもっともらしいままなので**値では気づけない**。
   ★そして**監査の結果は「28 項目」の行まで読むこと** ——
   時代別の表だけ見て「1 FAIL」と報告し、実際は 2 FAIL だった
56. ★**自動で落とす判断は、バナーと同じ条件にする。** 気候が解けないときの
   自動減速を `converged` だけで判断したら、**冥王代の 5.4 億年がずっと ×1**
   に張り付いた（マグマオーシャンは内部熱流 285 W/m² で平衡から遠いのが正常）。
   許容を強制項の大きさで決める式（`showOutOfRange` と同じ）に揃えたら、
   400 ティックで 7 回だけ発動して ×20 に復帰するようになった。
   ★**判断は【ソルバの報告】で行い、FPS では行わない** ——
   FPS で決めるとマシンの速さで物理が変わる（`docs/04-6`）

57. ★**対話用に打ち切った解を、バッチ用の基準で判定しない。** ブラウザは
   `maxOuter: 8` で Newton を打ち切っている（打ち切らないと 700ms/frame）。
   その残差を監査と同じ許容（1 W/m²）で測ったので、**冥王代の後期から
   ゲーム終了まで「気候が解けていません」が出続けた**。
   実測では**範囲外のセルは全史 0 件**で、壊れてはいなかった。
   **警告の条件は、その解が何を保証しているかに合わせること**
58. ★**外側の輪でアルベドを動かすと、平衡ソルバの残差は消えない。** 生物起源の
   CCN を `life.ts` から `globals` 経由でアルベドに足したら、太古代の不平衡が
   **5.6e-3 → 1.6 W/m²**（300 倍）になった。生物圏 → CCN → アルベド → 気温 →
   生物圏 の輪が **Newton の外**にあるため。★**階段を均しても効かない**
   （時定数 1/5/20 Myr で 1.6/1.5/1.8）。
   入れるなら**ソルバの内側で解く**しかない
59. ★**全史の中央値は、1 つの時代だけの異常を隠す。** 上の CCN のバグは
   監査の「エネルギー収支」を**中央値 2.2e-4 で PASS** させていた ——
   CCN がゼロでないのは太古代だけなので中央値に埋もれた。
   **時代分解で見ること**（トラップ 17 の再発）。
   そして**私は「打ち切りのせい」と 2 度説明して 2 度とも外した。**
   1 変数ずつ切り分けるまで、原因を語らないこと

60. ★**ディザは「境目」に掛けるもの。「なだらかな傾き」に掛けると全面が砂嵐になる。**
   混色を段に落として（18049 色 → 122 色）ドット絵にしたとき、端数を全部
   Bayer でディザしたら**海と極が一面の点々**になった。海の深さも氷の割合も
   **どこでも段の途中**なので、画面のすべてがディザの対象になる。
   氷は割合 0.1 の海面（＝ほぼ全部の海）に白い点が撒かれて最悪だった。
   ★**傾きはベタの段、ディザは二値のマスク（汀線）だけ。**
   ついでに、**陸海の重みだけは補間しない** —— `ss = 4` なら 1 セルが
   ちょうど 4x4 の Bayer に重なるので、陸の割合が**16 段のドットの密度**に
   なって面積が厳密に保たれる（補間すると汀線が 10px の砂粒の帯になった）
61. ★**「拡大するとぼやける」の原因は補間とは限らない。** `imageSmoothingEnabled`
   は最初から切ってあった。犯人は**場ではなく色の側**で、連続の混色が
   1 枚に 18049 色を作っていた。**まず色数を数えること**（`probe-pixel.ts`）。
   3.5 秒で測れて、ぼやけの有無が 1 つの数字になる

62. ★**`npm run smoke` は「押さないと出ない部品」を一度も写さない。** 通しの
   筋書きしか走らないので、レイヤの選択画面は作っても撮られなかった。
   罠 48（UI を足したらスクリーンショットを見る）を満たすには、
   **部品ごとに開いて撮る道具**が要る（`scripts/ui-shot.ts`）
63. ★**画面の端から逃がす高さを直書きしない。** 下端の帯に時間バーを
   取り込んで高さが 92 → 124px になった瞬間、`bottom: 92px` を書いていた
   **5 か所すべて**が帯の裏に隠れた（右の列の下・凡例・系譜・レイヤ選択・介入列）。
   `--bottom` の 1 変数にする。**同じ数字が 5 か所にあるなら、それは変数である**
64. ★**絶対配置で 2 枚を縦に積むと、中身が伸びた側が画面の外へ出る。**
   状態パネルを `max-height: 52vh`、年代記をその下に置いたら、
   **生命の 3 行が画面の外**に出た（値では気づけない —— スクロールすれば見える）。
   縦に積むものは flex の 1 本にして、**優先度の低い方に残りを取らせる**

65. ★**「液体の水が要る機構」を数え上げること。1 つ直すと次が出る。**
   冥王代の寒さを追って、**同じ形のバグが 2 つ**見つかった ——
   (a) 海底の炭酸塩化が、水がすべて水蒸気なのに走っていた
   (b) 炭素のリザーバ `Meff = 21 Gt-C/ppm` は**大気+海洋**で、
   **10 倍のうち 9 割は海の緩衝**。海が無い時代に 9 割を薄めていた。
   どちらも `oceanWaterFraction` の門を持っていたが、片方は
   **飽和が甘くて 0.10 しか絞れず**、もう片方は門が無かった。
   ★門の定義は **1 か所にまとめること**（`liquidWaterGate`）。
   別々に書くと片方だけ直したときに**打ち消し合って成功に見える**（19 の再発）
66. ★**罠 14 の点検で「較正の分母か」を見たら、`Meff` は分母ではなかった。**
   火山脱ガスは `volcanicFlux = land + seafloor` で定義され、`Meff` は
   そこに現れない。`Meff` が効くのは `dCo2 = net/Meff·dt` ——**応答の時定数だけ**で、
   基準状態では net = 0 なので姿を見せない。**だから安全に変えられた。**
   ★確かめ方は「現在の地球で 12 ステップ回して小数 6 桁まで一致するか」
   （数秒。罠 39 の「対照が本当に対照か」を値で確かめる）

67. ★**セーブの正しさは「もっともらしい値」では測れない。往復して 1 ビットか。**
   決定論が契約なので（`docs/04-6`）、判定は
   **「保存 → 読込 → N 歩」が「保存せず N 歩」と厳密に一致するか**しかない。
   そしてこのテストは**何を保存し忘れたかを教えてくれる**。実際に 4 つ出た:
   - **`SubsystemLoop.pending`**（サブシステムの端数の持ち越し）。
     これが「いつ発火するか」そのもの。落としたら 10 歩後に
     **マントル温度が 1770℃ と 1890℃**（冷却 1 段ぶん）に割れた
   - **`refWaterVolume`**（最初の 1 歩で 1 回だけ測る海水量の基準）。
     復元後は**そのときの地形で測り直して**海面が別の所に落ち着く
   - **型付き配列**。`Genome` の `Uint8Array` は JSON で `{"0":1,…}` になり、
     **添字で読めるので動いているように見える**。分岐と変異で初めて壊れる
   - **オブジェクトの共有**。`life.history` は生きているクレードと
     **同じオブジェクトを指している**。JSON を通すと共有が切れ、
     系統樹だけが保存時の姿で止まる。**物理は動くので画面を見るまで気づけない**
68. ★**`JSON.stringify(-Infinity)` は `"null"`。** `lastLipYear` のような
   「まだ一度も起きていない」を `-Infinity` で持つと、往復して `null` になり
   `year - null = year` という**有限の差**に化ける。非有限は文字列に逃がす
69. ★**保存の判定条件を、値ではなく「所有者」で書く。** 状態は
   **その状態のすぐ隣**に `snapshot()` / `restore()` を置くこと。
   `snapshot.ts` に全部書くと、あとで状態を足した人が直し忘れる

70. ★**汎用のクラス名を、後から別の意味で使わない。** `.panel .title` が
   パネルの見出しに使われているのに、最初の画面にも `.title` を付けたら、
   **解説パネルの見出しが画面いっぱいの半透明の板になった**
   （`position: absolute; inset: 0` を継いだ）。地図が透けて初めて気づいた。
   新しい全画面の部品は `.titlescreen` のように**用途を名前に入れる**
71. ★**`docs/` の文章をそのまま画面に出すと Markdown の記法が見える。**
   `**強調**` と `` `コード` `` が字のまま出た。変換を掛けること。
   ★**先にエスケープしてから変換する** —— 逆にすると本文が HTML を注入できる

72. ★**「入るか」は撮ってから言う。** スマホ幅 390px に対し、左の列 152 +
   右の列 300 + レイヤの選択 452 —— **どれ 1 つも入らない**。
   撮ったら地図がほぼ全部隠れていた。狭い画面では**畳むのではなく
   置き場所を変える** —— パネルを下から出るシートにして、一度に 1 枚だけ出す
73. ★**シートを `display: block` で出すと flex が死ぬ。** 介入の列は
   `flex-direction: row; flex-wrap: wrap` で 2 列に並べていたのに、
   可視化を `display: block` でやったせいで**縦 1 列・幅 103px に潰れた**。
   可視化は元の `display` の値で行うこと
74. ★**タッチの指は複数ある。** マウスは 1 本しか無いので `pointermove` を
   素直に書くと、2 本目が来たときに**片方の指だけを見て地図が飛ぶ**。
   `pointerId` で全部覚えること。★**ピンチを終えて指を離した瞬間を
   クリックにしない** —— 拡大するたびに介入が落ちる

75. ★**`public/` は開発用の素材まで本番に載る。** `dist` が 46.9MB あり、
   うち **38MB が `timelapse.bin`**（開発用の閲覧ページだけが読む）。
   Cloudflare Pages は **1 ファイル 25MiB 上限**なので、そのままではデプロイが
   失敗する。`scripts/prune-dist.ts` で落として **0.4MB** になった。
   ★**上限の検査もその場に書くこと** —— 落とし忘れたら次に気づくのは
   デプロイの最後になる
76. ★**開発サーバが通っても本番ビルドが通るとは限らない。** ヘッダの出所が
   違う（dev は `vite.config.ts`、本番は `_headers`）。
   `scripts/serve-dist.ts` で `dist` を `_headers` どおりに配って、
   **同じ smoke を掛ける**こと。実測で `crossOriginIsolated: true` を確認した
77. ★**COOP/COEP が欠けても落ちない。遅くなるだけ。** `SharedArrayBuffer` が
   使えないと場が毎フレーム転送される。**気づきにくい失敗**なので、
   通し確認で `self.crossOriginIsolated === true` を明示的に検査する
78. ★**JSDoc の中に `*` と `/` を並べない。** `**/*.aseprite` と書いたら
   そこでコメントが閉じて、esbuild が `Unexpected "*"` で落ちた

79. ★**「まったく増えない」は、増えていないとは限らない。画面が止まっていた。**
   章立ての早送りは進捗バーだけ返して**状態を返していなかった**ので、
   気温も CO2 も O2 も着くまでの十数分〜1 時間ずっと初期値のままだった。
   実測すると酸素は**ちゃんと増えていた**（128x64 で GOE 4/4）。
   ★**惑星が動いていることと、画面がそう言っていることは別**
80. ★**整数に丸める操作は、小さい入力を丸め戻して「動かなく」する。**
   拡大の倍率を整数（アート 1px = 画面 n px）にスナップしたら、
   ホイール 1 段（1.17 倍）が次の整数に届かず**同じ段に戻って拡大が効かなくなった**。
   丸めた結果が動かなかったら**段そのものを 1 つ進める**こと。
   ★押しても何も起きない類のバグは型検査にもスクショにも出ない。**数で押さえる**
81. ★**`<select>` の値に無い選択肢を代入すると、黙って空文字になる。**
   96x48 の章を読み込むとき `gridSelect.value = "96x48"` としたが選択肢に無く、
   `"".split("x")` から **`0 × undefined` で生成が止まった**（画面は
   「惑星を生成しています…」のまま）。**読み込んだ物の寸法を UI の選択肢に縛らない**
82. ★**面積に忠実な描画が、いちばん嘘に見えることがある。** 45 億年後の惑星は
   陸が薄く広がり、実測でセルの **44% が 2/16〜6/16**、12/16 超は **0.1%**。
   セル内ディザは面積に忠実だが、そういうセルを**散らばった 3〜6 ドット**として
   描くので惑星が**緑の紙吹雪**になった。しかも**数字と食い違っていた** ——
   ダッシュボードは `elevation >= 0` で陸 6.5% なのに地図は 18.6% を撒いていた。
   ★**地図は数字と同じことを言うこと。** 補間してから 0.5 で切る

83. ★★**物理が食べている量を、測定が見ていなかった。** 気候のアルベドも
   炭素の風化も**サブグリッドの `landFraction`** を読んでいるのに、
   監査もダッシュボードもずっと **`elevation >= 海面`**（セル平均の離散判定）を
   見張っていた。**2 つは「まとまった陸が消えると」開く** —— 実測（96x48・4 seed）
   で冥王代は比 1.00〜1.12 なのに原生代で **最大 3.48**。
   半分が厚い地殻・半分が薄いセルでは、セル平均は海面を下回るのに
   粒子の半分は海面より上にある（罠 23 と同じ形）。
   ★**定義は 1 か所に**（`landAreaFraction`）。2 つあったから起きた
84. ★**`scripts/` が型検査に入っていない**（`tsconfig.json` の include は
   `src` と `tests` だけ）。存在しない `w.grid.totalAreaWeight` を書いても
   通った。**監査とプローブは全部の判断の土台なのに、そこだけ型が見ていない。**
   `@types/node` が要るので依存が増える。入れるまでは**走らせて確かめる**こと

85. ★★**主指標が 1 年近く全部 0 だった。** 起伏の「分散の台帳」（どの機構が
   起伏を作り、どれが潰しているか）は**格子ベースの地殻モデルの中にしか無く**、
   粒子モデルは `if (p.crustModel > 0) { ...; return }` で**その手前で戻って**いた。
   地殻を粒子にした日（2026-08-29）から死んでいたのに、
   `probe-relief.ts` は「M4 の主指標」と書いたまま 0 を印字し続けていた。
   ★**0 が並んでいたら「効いていない」ではなく「測れていない」を先に疑う**
   （罠 46 の純粋形）。分岐でモデルを差し替えたら、**診断も差し替わったか**を見る
86. ★**途中経過を測るには、途中でラスタライズし直すこと。** 台帳は
   `crustThickness` の【場】を読んでいたが、その場は**ステップの最後に 1 回**
   しか粒子から作られない。機構が動かすのは粒子の方なので、
   途中で場を測っても値は変わらず**差が全部 0 になる**。
   費用がかかるので既定は切り、測るときだけ立てる（`varLedgerEnabled`）

詳しい実測値と経緯は `WORK-IN-PROGRESS.md` と `docs/01-6.5c`。

## 設計上の契約

- **解像度独立**（`docs/04-8.8`）。新しい量は必ず物理単位で書く
  （速度は km/yr、密度は kg/m²、確率は単位面積・単位時間あたり）
- **決定論**（`docs/04-6`）。マシンの速さで物理が変わってはいけない。
  粒子は添字順に走査し、生成は free リストの LIFO にすること（`src/sim/parcels.ts`）
- **収支を閉じる。** 新しい保存量を足す時点で収支検査も同時に足す
- **炭素の較正は「現在の地球は定常」を火山脱ガス量の定義に使う。**
  だから較正は必ず現在の地球の条件で行う（`carbon.ts` の `assertReferenceState`）

## ドキュメント

| | 内容 |
|---|---|
| `docs/00-concept.md` | 何を作るか |
| `docs/01-simulation-model.md` | 物理モデルと調査の記録 |
| `docs/02-life-and-civilization.md` | 生命・文明の設計 |
| `docs/03-gameplay.md` | ゲームとしての設計 |
| `docs/04-architecture.md` | 実装の構造 |
| `docs/05-roadmap.md` | マイルストーン。**いまどこかはここ** |
| `docs/06-science-basis.md` | 科学的な根拠 |
| `docs/07-art-spec.md` | ドット絵アイコンの発注仕様 |
